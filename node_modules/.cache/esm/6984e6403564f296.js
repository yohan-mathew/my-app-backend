let User;_356‍.x([["userInfo",()=>userInfo],["userInfo",()=>userInfo]]);_356‍.w("../models/user",[["default",["User"],function(v){User=v}]]);

        const userInfo = async (req, res,next) => {
    try{const query = User.find({}, "name"); // Create the query
    const users = await query.exec(); // Execute the query
    _356‍.g.console.log(users);
    const names = users.map((user) => user.name);
    res.json(names)}
    catch(error){
        next(error)
    }
 }

 