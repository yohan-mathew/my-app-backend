let User;_a90‍.x([["userInfo",()=>userInfo],["userInfo",()=>userInfo]]);_a90‍.w("../models/user",[["default",["User"],function(v){User=v}]]);

        const userInfo = async (req, res,next) => {
    try{const query = User.find({}); // Create the query
    const users = await query.exec(); // Execute the query
    _a90‍.g.console.log(users);
    const names = users.map((user) => ({name:user.name, barber:user.barber, time:user.createdAt}));
    res.json(names)}
    catch(error){
        next(error)
    }
 }

 