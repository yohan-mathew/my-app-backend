_307‍.x([["adminlog",()=>adminlog],["adminlog",()=>adminlog]]);const jwt= require('jsonwebtoken')


       const adminlog = async (req, res, next) => {
    try{
        const {email, password} = req.body;

        //bad request (user not authorized)
        if (email != process.env.NAME || password != process.env.PASSWORD){
            console.log(email)
            console.log(password)
            throw new Error("user not authorized")
        }

        
        res.cookie('loggedIn', true);

        res.send("welcome suzi")
        
    }
    catch(error) {
        next(error)
    }
};

